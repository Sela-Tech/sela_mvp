{
  "name": "Sela",
  "version": "1.0.0",
  "description": "Sela: A Public Project Monitoring & Micropayment DApp",
  "keywords": [
    "ethereum",
    "web3"
  ],
  "main": "./src/js/app.js",
  "scripts": {
    "clean": "rm -r ./build/contracts/*",
    "compile": "truffle compile",
    "deploy": "truffle migrate --reset --compile-all",
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install web3",
    "ganache": "ganache-cli"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "web3": "1.0.0-beta.30"
  },
  "devDependencies": {
    "ethereumjs-testrpc": "*",
    "lite-server": "^2.3.0",
    "truffle": "*"
  }
}
